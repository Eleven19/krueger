// Stub for Scala.js Node test runtime.
//
// The `krueger.webapp` module has `@JSImport("monaco-editor", JSImport.Namespace)` in MonacoFacade, which makes
// Scala.js emit an ES `import` statement at the top of the test bundle. Node cannot resolve `monaco-editor` during
// `./mill krueger.webapp.test` because the real package is an npm dep of the docs site, not the webapp test module.
//
// Tests never invoke the Monaco API at runtime — MonacoPlan is tested purely, MonacoEditor.apply is only reached in
// the browser. This stub exists solely so the top-level import resolves. Production builds pull the real
// `monaco-editor` via the docs site's Vite config and this stub is not consulted.
export const editor = { create: () => ({ getValue: () => "", setValue: () => {}, onDidChangeModelContent: () => {}, dispose: () => {} }) };
export const languages = { register: () => {}, getLanguages: () => [], setMonarchTokensProvider: () => {} };
export default { editor, languages };
