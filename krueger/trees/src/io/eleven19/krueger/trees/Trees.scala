package io.eleven19.krueger.trees

/** Entry point for the krueger.trees module.
  *
  * Hosts the generic `QueryableTree[T]` typeclass and the tree-sitter-inspired query DSL. Filled in by downstream
  * issues in the krueger-trees epic; this placeholder exists so the module compiles during scaffolding.
  */
object Trees:
    val moduleName: String = "krueger-trees"
