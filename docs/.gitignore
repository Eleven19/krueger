node_modules/
dist/
.astro/
.env
.env.production
.DS_Store

# Generated Scaladoc trees and landing page written by `./mill docs.writeToDocsPublic`.
# Astro copies `public/` verbatim into `dist/`, so these are regenerated on every build.
/public/api/

# SvelteKit playground static output mirrored by `./mill docs.prepareLocalDevSite`.
# (Source lives in `sites/try-wasm/`; deployment URL is `/krueger/try/`.)
/public/try/
