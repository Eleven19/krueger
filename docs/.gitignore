node_modules/
dist/
.astro/
.env
.env.production
.DS_Store

# Generated Scaladoc trees and landing page written by `./mill docs.writeToDocsPublic`.
# Astro copies `public/` verbatim into `dist/`, so these are regenerated on every build.
/public/api/

# Laminar playground Scala.js bundle written by `./mill krueger.webapp.writeToDocsPublic`.
# Astro copies `public/` verbatim into `dist/`, so this is regenerated on every build.
/public/try/app/
